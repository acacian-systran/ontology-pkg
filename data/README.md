# data/

`packages.db` — 책의 `retail.sql` 에 대응하는 고정 관계형 원천 ([7.1]).

**한번 만들면 고정한다.** 7장부터 12장까지 같은 원천을 쓴다.
중간에 데이터를 바꾸면 매핑·추론·성능 결과를 서로 비교할 수 없게 된다.

스키마는 `CLAUDE.md` 의 표가 출발점이고, 1주차 CQ 를 쓴 뒤 확정한다.
원본 응답(JSON 등)은 `data/raw/` 에 그대로 남긴다 — [12.05] 재현성 매니페스트의 재료다.

<!-- 절 링크 — references/book.md 에서 나온다 -->
[7.1]: https://wikidocs.net/389958 "7.1 패러다임 차이와 고정 SQLite 원천"
[12.05]: https://wikidocs.net/390006 "12.05 재현성 매니페스트"
