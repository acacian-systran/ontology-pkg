# scripts/

수집·적재·실행 하네스. **모델링이 들어가는 코드는 여기 없다.**

- 수집 — 레지스트리에서 원본 받기
- 적재 — `data/packages.db` 만들기
- 하네스 — RDFLib 파싱, owlrl 추론, pySHACL 검증을 한 번에 돌리고 결과를 읽기 좋게 뽑기
- ER — blocking 과 점수 계산 (4주차)

**LLM API 를 호출하는 코드를 두지 않는다.** 4주차 임베딩은 로컬 모델로만 한다.
